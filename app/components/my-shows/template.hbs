<hr>
<h2>{{fa-icon "fa-calendar-plus-o"}} My Shows</h2>

<p>
  Anywhere you see a calendar icon attached to a show, just click/tap on it and that show
  will be added to your custom list of shows here on the home page. This will make it easy
  for you to keep track of the shows you want to see.
</p>


{{#if myShows.shows.length}}
  <ol class="no-bullets">
  {{#each myShows.shows as |event|}}
    <li class='small-12 columns accordion event-row' data-talk-id={{event.id}} data-event-row-id={{event.id}} onclick={{action "chooseShow"}}>
      <div class="row small-collapse medium-uncollapse">

        <div class="band-name small-7 medium-5 columns">
          <span data-event-id={{event.id}}>{{event.bandName}}</span>
          {{!is-interested-button talkId=talk.id}}
        </div>
        <div class="event-time small-5 medium-3 columns">
          <span>{{short-day event.day}} <span class="time">{{event.time}}</span></span>
        </div>
        <div class="stage-name small-7 medium-3 columns">
          <span>{{event.stageName}}</span>
          {{!is-interested-button talkId=talk.id}}
        </div>
        
        <div class="event-time small-5 medium-1 columns" onclick={{action "removeShow" event}}>
          <span class="remove">{{fa-icon "fa-calendar-times-o"}}</span>
        </div>
      </div>
    </li>
  {{/each}}
  </ol>
{{else}}
  Mark the shows you want to see and we'll keep track of them here!
{{/if}}
{{yield}}
<hr>
