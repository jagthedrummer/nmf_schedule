import { shortDay } from '../../../helpers/short-day';
import { module, test } from 'qunit';

module('Unit | Helper | short day');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = shortDay([42]);
  assert.ok(result);
});
