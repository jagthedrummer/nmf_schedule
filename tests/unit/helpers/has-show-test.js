import { hasShow } from '../../../helpers/has-show';
import { module, test } from 'qunit';

module('Unit | Helper | has show');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hasShow([42]);
  assert.ok(result);
});
